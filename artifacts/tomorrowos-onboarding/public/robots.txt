# Production robots policy (see LAUNCH_CHECKLIST.md).
# Crawling is allowed everywhere; per-route indexing is controlled by
# page-level robots meta tags generated from src/lib/seoConfig.ts
# (non-indexable routes emit an explicit "noindex, follow").
User-agent: *
Disallow:

Sitemap: https://tomorrowos.org/sitemap.xml
